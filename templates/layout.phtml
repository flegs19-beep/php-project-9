<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Анализатор страниц</title>
    <link rel="stylesheet" href="/app.css">
</head>
<body class="min-h-screen flex flex-col">
    <nav class="bg-gray-800 text-white">
        <div class="px-4 py-4 flex gap-6 items-center">
            <a href="<?= $router->urlFor('home') ?>" class="text-xl font-semibold">Анализатор страниц</a>
            <a href="<?= $router->urlFor('urls.index') ?>" class="text-gray-400">Сайты</a>
        </div>
    </nav>

    <main class="flex-1">
        <?php foreach ($flash->getMessages() as $type => $messages): ?>
            <?php foreach ($messages as $message): ?>
                <div role="alert" class="max-w-6xl mx-auto mt-4 px-4 py-4 rounded <?=$type === 'success' ? 'bg-green-100' : ($type === 'error' ? 'bg-red-100' : 'bg-yellow-100')?>"><?= htmlspecialchars($message) ?></div>
                <?php endforeach; ?>
        <?php endforeach; ?>

        <?= $content ?>
    </main>

    <footer class="border-t py-5 text-center">
        <a href="https://ru.hexlet.io/" class="text-blue-600 underline">Hexlet</a>
    </footer>
</body>
</html>